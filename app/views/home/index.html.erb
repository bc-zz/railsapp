<h1>Home#index</h1>
<p>Find me in <code>app/views/home/index.html.erb</code></p>

<p>Coffeescript is in app/assets and is included in this page via the application layout file in <code>app/views/layouts/application.html.erb</code></p>

<p>The <code><%%= javascript_include_tag 'application', 'data-turbolinks-track' => true %></code> in the head of the application layout basically includes the 'application' bundle which is defined in <code>app/assets/javascripts/application.js</code></p>

<p>This page is action is set as the root route in <code>config/routes.rb</code></p>